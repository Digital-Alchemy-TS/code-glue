import { TServiceParams } from "@digital-alchemy/core";

export function ContextBuilder() {
  function build(): TServiceParams {
    // logger
    // context
    // hass
    // automation (partial)
    // shared (variables)
    // scheduler
    // helpers (entities)
    return undefined;
  }
  return { build };
}
